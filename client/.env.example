# Rename this file to ".env" for local overrides (optional — defaults to localhost:5001).
# On Vercel, set this in Project Settings → Environment Variables to your deployed
# backend URL (e.g. https://your-app.onrender.com), with NO trailing slash.
VITE_SERVER_BASE=http://localhost:5001
